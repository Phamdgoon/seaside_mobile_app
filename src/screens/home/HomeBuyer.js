import { View, Text } from "react-native";
const HomeBuyer = ({ navigation }) => {
    return (
        <View>
            <Text>Hello homebuyer</Text>
        </View>
    );
};

export default HomeBuyer;
