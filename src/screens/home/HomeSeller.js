import { View, Text } from "react-native";
const HomeSeller = ({ navigation }) => {
    return (
        <View>
            <Text>Hello homeseller</Text>
        </View>
    );
};

export default HomeSeller;
